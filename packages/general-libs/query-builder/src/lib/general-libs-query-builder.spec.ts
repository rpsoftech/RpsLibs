import { generalLibsQueryBuilder } from './general-libs-query-builder';

describe('generalLibsQueryBuilder', () => {
  it('should work', () => {
    expect(generalLibsQueryBuilder()).toEqual('general-libs-query-builder');
  });
});
